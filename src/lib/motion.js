// One easing curve for every animation on the site — originally the
// name's reveal-mask curve, now the shared signature so scroll-triggered
// fades and the Hero's cascade all move with the same feel.
export const EASE = [0.22, 1, 0.36, 1];
